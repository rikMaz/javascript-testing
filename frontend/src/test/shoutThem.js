export default (array) => array.map(item => item.toUpperCase() + "!!")

/*
export default function shoutThem(array) {

    let newArray = array.map(item => item.toUpperCase() + "!!")

    return newArray;
}*/
